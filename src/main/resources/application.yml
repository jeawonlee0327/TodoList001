spring:
  datasource:
    url: jdbc:postgresql://db.rrmjfwtoaeyrqrmteceb.supabase.co:5432/postgres
    username: postgres
    password: shgusdud744@

  jpa:
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true
        use_sql_comments: true

logging:
  level:
    org:
      hibernate:
        SQL: debug
        orm:
          jdbc:
            bind: trace